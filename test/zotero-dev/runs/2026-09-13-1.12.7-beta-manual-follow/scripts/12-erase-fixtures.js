return (async()=>{const root=Zotero.__ztts127,ids=[];for(const kind of ['pdf','epub']){const id=root&&root[kind]&&root[kind].itemID;if(id&&ids.indexOf(id)<0)ids.push(id)}const out=[];for(const id of ids){const item=Zotero.Items.get(id);let error=null;try{if(item)await item.eraseTx()}catch(e){error=String(e)}out.push({id,erased:!Zotero.Items.get(id),error})}await new Promise(q=>setTimeout(q,550));return JSON.stringify({out})})()
