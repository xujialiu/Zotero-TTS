return (async()=>{const root=Zotero.__ztts100;const reader=root.pdf.reader;const internal=reader._internalReader;const manager=internal._readAloudManager;const view=internal._primaryView;const rw=view._iframeWindow;const host=reader._window;const container=rw.document.getElementById('viewerContainer');const index=(Zotero.Reader._readers||[]).indexOf(reader);const snap=label=>{const d=JSON.parse(Zotero.ZoteroTTS.diagnostics.autoScroll())[index]||null;return{label,at:Date.now(),position:manager._controller?manager._controller._position:null,scrollTop:container.scrollTop,following:d?d.following:null,interacting:d?d.interacting:null,keep:d?d.keepFollowingWhileVisible:null,reason:d?d.reason:null,last:d?d.last:null}};Zotero_Tabs.select(reader.tabID);reader.focus();rw.focus();const events=[];const listener=e=>events.push({trusted:!!e.isTrusted,deltaY:e.deltaY,target:String(e.target&&e.target.localName||'')});container.addEventListener('wheel',listener,true);const before=snap('before');let wheelError=null;try{const frame=rw.frameElement.getBoundingClientRect();host.windowUtils.sendWheelEvent(Math.round(frame.x+100),Math.round(frame.y+500),0,220,0,0,0,0,0,0)}catch(e){wheelError=String(e)}const immediate=snap('after-trusted-wheel');container.scrollTo(0,20);const afterMove=snap('after-controlled-visible-move');await new Promise(resolve=>setTimeout(resolve,300));const settled=snap('settled');container.removeEventListener('wheel',listener,true);return JSON.stringify({wheelError,events,before,immediate,afterMove,settled,actualDelta:afterMove.scrollTop-before.scrollTop})})()
