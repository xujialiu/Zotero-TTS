return (async () => {
  const root = Zotero.__ztts100, reader = root?.epub?.reader;
  if (!reader) throw new Error('EPUB reader is missing');
  const internal = reader._internalReader, manager = internal?._readAloudManager, view = internal?._primaryView, rw = view?.iframeWindow;
  if (!manager || !view || !rw) throw new Error('EPUB paginated view is not ready');
  const pref = 'extensions.zotero.zotero-tts.readAloud.autoScrollMode', original = { value: Services.prefs.getStringPref(pref, 'sentence'), user: Services.prefs.prefHasUserValue(pref) }, sleep = ms => new Promise(resolve => setTimeout(resolve, ms));
  const index = () => (Zotero.Reader._readers ?? []).indexOf(reader), diagnostic = () => JSON.parse(Zotero.ZoteroTTS.diagnostics.autoScroll())[index()] ?? null;
  const snap = phase => { const d = diagnostic(), helper = Components.utils.waiveXrays(view._readAloud), state = Components.utils.waiveXrays(helper?.state), selector = helper?._resolveSegmentSelector(state), range = selector ? view.toDisplayedRange(selector) : null, list = range?.getClientRects?.(), boxes = []; for (let i = 0; list && i < list.length; i++) { const b = list[i]; boxes.push([b.left, b.top, b.right, b.bottom]); } return { phase, at: Date.now(), flow: view.flowMode, offset: view.flow?._offsetLeft ?? null, section: view.flow?._currentSectionIndex ?? null, position: manager?._controller?._position ?? null, rects: boxes, visibleFragments: boxes.filter(b => b[2] > 0 && b[3] > 0 && b[0] < rw.innerWidth && b[1] < rw.innerHeight).length, following: d?.following ?? null, interacting: d?.interacting ?? null, pending: d?.pending ?? null, reason: d?.reason ?? null, last: d?.last ?? null }; };
  const explicitLock = async label => { let error = null; try { Components.utils.waiveXrays(view._readAloud).setPositionLocked(true); } catch (e) { error = String(e); } await sleep(350); return { label, error, after: snap(label + '-after') }; };
  const navigation = async () => { const before = snap('navigate-before'); let callError = null, result = null; try { result = view.navigateToNextPage(); } catch (e) { callError = String(e); } const immediate = snap('navigate-immediate'); let settled = null; if (result?.then) { try { settled = await Promise.race([Promise.resolve(result).then(() => 'resolved', e => 'rejected: ' + String(e)), sleep(1800).then(() => 'timeout')]); } catch (e) { settled = 'await-error: ' + String(e); } } await sleep(260); return { callError, returnedPromise: !!result?.then, settled, before, immediate, after: snap('navigate-after') }; };
  Zotero_Tabs.select(reader.tabID); reader.focus?.(); rw.focus?.();
  let output = null;
  try { Services.prefs.setStringPref(pref, 'outside'); try { await view.setFlowMode('paginated'); } catch (e) {} await sleep(700); try { manager.repositionTo(50); } catch (e) {} await sleep(650); try { if (manager.active && !manager.paused) manager.pause(); } catch (e) {} const setup = snap('setup'); manager._stateChanged(); await sleep(250); const automaticStatePush = snap('automatic-state-push'); const nav = await navigation(); const lock = await explicitLock('lock-after-navigation'); try { manager.repositionTo(50); } catch (e) {} await sleep(500); try { if (manager.active && !manager.paused) manager.pause(); } catch (e) {} output = { mode: 'outside', setup, automaticStatePush, navigation: nav, lockAfterNavigation: lock }; }
  finally { try { if (manager.active && !manager.paused) manager.pause(); } catch (e) {} try { await view.setFlowMode('paginated'); } catch (e) {} await sleep(250); if (original.user) Services.prefs.setStringPref(pref, original.value); else if (Services.prefs.prefHasUserValue(pref)) Services.prefs.clearUserPref(pref); await sleep(180); }
  return JSON.stringify({ output, restoredMode: { value: Services.prefs.getStringPref(pref, '<none>'), user: Services.prefs.prefHasUserValue(pref) }, final: snap('final') });
})()
