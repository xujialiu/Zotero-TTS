JSON.stringify((()=>{const c=Zotero.Reader._readers[0]._internalReader._readAloudManager._controller;const pairs=[];for(const j of [6650,6654]){const a=c._audioBuffers.get(6648),b=c._audioBuffers.get(j);let different=0;if(a.length===b.length&&a.numberOfChannels===b.numberOfChannels){for(let k=0;k<a.numberOfChannels;k++){const x=a.getChannelData(k),y=b.getChannelData(k);for(let n=0;n<x.length;n++)if(x[n]!==y[n])different++;}}else different=-1;pairs.push({a:6648,b:j,length:a.length,channels:a.numberOfChannels,differentSamples:different});}return pairs;})())
