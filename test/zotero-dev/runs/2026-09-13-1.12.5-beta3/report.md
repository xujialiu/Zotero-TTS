# Issue #95 live verification — sanitized run evidence

## Result table

| Check | Observed | Expected | Status |
| --- | --- | --- | --- |
| Build identity | Zotero `10.0.2-beta.9+c77df79af`, Firefox 140, Windows; plugin `1.12.5-beta3`; XPI SHA-256 `1A3458649503A75D8706E743D23F840E36FE257D76DDEF121DF0D107D98C4BE5`; bundle `content/zotero-tts.js` SHA-256 `CD9A1F786FCCC6A24891F9705B177006765669FA1A138F8F3284AD023FF13721` | Exact beta3 build installed | PASS |
| Startup and mechanism | `diagnostics.startup()` reported version `1.12.5-beta3`, 23 startup steps `ok`, including `prepared voice switching`, and `failed: []`; `diagnostics.voiceSwitch()` reported `mechanism: prepared-native-voice-v1` and bindings `Shift+,` / `Shift+.` | Native mechanism is present and startup has no failed step | PASS |
| Baseline | One pre-existing user reader remained active and paused at position 517; settings window was open on General; debug storage was already true; position database had 66 rows, queue 0, `lastError: null`. Snapshotted preference values and user-value flags were retained without printing raw user voice state. | Record and preserve user state | PASS |
| Audio clock and trusted start | Fresh fixture started with trusted `Shift+Space`; the keydown was consumed (`1`), the native `AudioContext` changed from `suspended/0` at the first sample to `running`, and its clock advanced to about `2.93 s` while position advanced from 0 to 1. | Playback checks require a moving native clock | PASS |
| Filtered menu and trusted punctuation keys | Controlled native menu was `[native95-a, native95-b, native95-c, native95-d]` in player order. Previous `b→a`, previous wrap `a→d`, and next wrap `d→a`; each trusted punctuation key returned `[0,1,true,true]`. | Follow the current filtered list and wrap at both ends | PASS |
| Paused selection and sample suppression | Every list transition stayed `active:true, paused:true`; native transport recorded zero `sample` calls. | A paused pick changes voice silently | PASS |
| Single-voice list | A temporary attempt to replace the reader's internal voice array was rejected by the live reader; the observed menu stayed length 4. | Empty or one-voice list remains unchanged | NOT TESTABLE (unit-only coverage) |
| Rebinding, clearing, repeat and editable field | Rebound next to `Shift+F9`: `b→c`; clearing it made `Shift+F9` return `keydown:0` with no change. A repeated punctuation key did not advance a second time. An editable input caused `keydown:2` and left the voice unchanged. Original next binding restored as `Shift+.` with `user:false`. | Preserve routing, ignore repeats/editable targets, and honor rebind/clear | PASS |
| Regional list | The controlled catalog exposed one `en-GB` voice, one `en-AU` voice and four `en-US` voices; the reader resolved the corresponding region lists and the original region/list was restored. | Use the player's regional filtered list | PASS (controlled native list) |
| Favorites-only filtering | The direct native transport fixture bypassed the plugin's favorites filter, so toggling the preference could not prove the actual composite player's filtered list. | Favorites-only list changes the navigable menu | NOT TESTABLE (unit/full voice-browser coverage) |
| Word handoff | With target audio delayed 700 ms, old `native95-a` remained selected and alive through preparation. Diagnostic stage moved `preparing→word`; old source scheduled numeric `stop(1.260666...)` at old clock `0.992`. The native controller then selected `native95-b`; `last` was `{kind:"word", index:0, charStart:22, offset:1.25}` and the prepared controller's first play used offset `1.25`. The current target segment was fetched once and no sample was requested. A follow-up probe confirmed the active segment object retained its `sourcePosition` and was identical to the manager's segment 0. | Old audio stops at the scheduled word end, then the prepared native controller starts at the new voice's matching next-word offset with the original source position | PASS |
| Sentence fallback | With target timestamps omitted, old audio continued to the next segment; diagnostic `last` was `{kind:"sentence", index:1, offset:0}`. Target current and next sentences were requested once each, with no early word stop or sentence repeat. | Missing/unreliable timing falls back to a native sentence boundary at offset 0 | PASS |
| Preparation overtaken | With old fixture audio shortened to 0.4 s and only target voice `native95-b` delayed 1.2 s, old position advanced `0→6`; prepared indices advanced `[0]→[0,7]`, then commit occurred at index 7. | Preparation moves farther ahead and never adopts behind the current reading position | PASS |
| Rapid presses | Two trusted next presses 35 ms apart left `pending:native95-c` while `native95-a` was still selected; the final native handoff committed to `native95-c` and no stale target played. | Latest pending target wins | PASS |
| Return-to-current cancellation | A pending `native95-b` followed by previous back to current `native95-a` immediately produced `stage:"cancelled", pending:null`; old controller remained alive and no target-b request was made. | Returning to the current voice cancels pending work | PASS |
| Pause cancellation | Pending target preparation was cancelled synchronously by `manager.pause()`; after the delayed response, selected voice and controller were still the old ones and `pending:null`. | Pause removes pending work and stale results never play | PASS |
| Preparation failure | Target `native95-b` returned a controlled rejection. Diagnostic stage was `failed`; old `native95-a` remained selected, active, unpaused and the same controller; toast text was `Could not switch to Native Fixture B. Try again, or pause before changing the voice.` | Failure leaves old audio playing and reports a failure | PASS |
| Armed-stop cancellation | Numeric early stop was observed at `0.366666...` with clock `0.221333...`; pause left `pending:null`, `stage:"cancelled"`, old controller alive and context running. The live wrapper did not expose the later numeric reschedule separately from native pause's `stop(undefined)`. | Cancelling after arming must move the old stop beyond natural completion | NOT TESTABLE (unit test covers late reschedule) |
| Speed/skip/manual voice/stop/tab close/shutdown cancellation | The beta3 unit suite covered these cancellation paths; no separate live run was accepted because each would require additional lifecycle manipulation of the fixture. | Every listed action cancels pending work | NOT TESTABLE (unit-only) |
| One voice everywhere, on/off, two playing tabs | Two-fixture exploratory scripts could not keep both native fixture managers playing concurrently through this Zotero session; their shared-voice traces were therefore not used as PASS evidence. | With the switch on, another playing tab prepares at its own boundary; off leaves it unchanged; paused tabs stay silent | NOT TESTABLE (unit/exploratory only) |
| Cleanup and restoration | Both fixture readers were closed and both imported items erased. No fixture reader/item remained; position rows returned to 66, queue 0, `lastError:null`. All snapshotted preference values and user-value flags matched baseline; raw `readAloud.memory` and `reader.readAloudVoices` were only compared by equality/length. The user reader was explicitly resynchronized to its original Fish voice, active+paused at position 517. Settings stayed open on General and debug storage stayed true. | No pending request, retained controller, injected method, fixture or changed preference remains | PASS |
| Error review | No `[zotero-tts]` error was left by the final native path. The console also contained expected controlled failure (`Failed to fetch audio`), fixture lifecycle/page teardown noise (`InvalidStateError: Navigated away from page`, missing PDF viewport during teardown), autoplay warnings from scripted popup starts, one corrected first-run harness error (`invalid assignment to const 'out'`), and unrelated Zotero sync restarts/HTTP 502. | Distinguish plugin errors from harness/Zotero background noise | PASS with recorded harness noise |

## Scripts and sanitized evidence

The exact executed scripts are retained in [test/zotero-dev/runs/2026-09-13-1.12.5-beta3/scripts](scripts/README.md). The sequence was baseline, sync/mute, fixture import/readiness, native transport setup, trusted clock start, word handoff, menu/key checks, sentence fallback, cancellation/failure, regional probe, second fixture setup and exploratory shared-voice attempts, overtaken preparation, and cleanup. The exploratory shared scripts remain linked for reproducibility but are explicitly excluded from the PASS table.

The controlled transport generated silent WAV fixtures and timestamps from the actual fixture segment text. It did not call a paid provider or fabricate timestamps for real speech. The native `AudioContext` clock was running during the accepted word, sentence and overtaken checks. Subjective pronunciation, perceptual gap and naturalness remain human-only.

The beta2 build was installed and its startup diagnostic passed before the parent session produced beta3; no beta2 behavior result was retained after the scheduling fix. The accepted behavior evidence in this directory is for beta3 above.

## Restoration note

The baseline had `readAloud.volume=100/user:false`, `sameForAllDocuments=true/user:false`, `globalSpeed=true/user:false`, `favoritesOnly=false/user:false`, sentence delay enabled at 0 ms, paragraph delay enabled at 200 ms, default previous/next bindings, `syncPositions=true/user:true`, `autoUploadSettings=true/user:true`, `syncSettings=false/user:false`, and user values for favorite voices, memory and the native voice map. Their raw contents are intentionally absent from this artifact.

One settings auto-upload was reported after the baseline values were restored (`pending:false`, `lastError:false`). The final local values and user-value flags are exact; this upload is recorded so the main session can distinguish it from a clean no-upload run.
