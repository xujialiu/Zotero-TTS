[Checklist index](README.md)

## 7. Errors and the end of a pass

After the last section: the debug store read for anything from the
plugin (`[zotero-tts]`, `zotero-tts.js` in a stack, dead objects), the
error console's contents against the baseline's, every pref
byte-identical, no fixture item left, the settings window closed, the
main window as found. Then one pass over whatever was fixed along the
way, from its section's first item.
