node_modules/
build/
addon/content/zotero-tts.js

# Agent scratch space and the rendered docs (npm run docs) — never committed.
.superpowers/
docs/

# Private notes (billing account ids, cloud setup) — never committed.
secrets/
