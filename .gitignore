node_modules/
build/
addon/content/zotero-tts.js

# Agent scratch space and the rendered docs (npm run docs, npm run site) — never committed.
.superpowers/
docs/
site/
.tmp/

# Private notes (billing account ids, cloud setup) — never committed.
secrets/

# Icon design drafts at the repo root (scratch, never committed); the plugin's
# own icons live in addon/content/icons/ and ship (issue #60).
/icons/
