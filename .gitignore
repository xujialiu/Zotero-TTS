node_modules/
build/
addon/content/zotero-tts.js

# Agent scratch space and the rendered docs (npm run docs, npm run site) — never committed.
.superpowers/
docs/
site/

# Private notes (billing account ids, cloud setup) — never committed.
secrets/

# Icon design drafts (npm-less scratch, never committed).
icons/
