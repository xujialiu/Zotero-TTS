# Zotero-TTS — engineering log, 2026-09-26

[← 2026-09-25](NOTES_2026-09-25.md) · [Index](NOTES.md)

## Zotero's H and U annotate through two reader calls, and the segment must be the manager's own (2026-09-26 01:55, issue #145)

A reporter found H and U no longer annotating during playback with the
plugin on. The plugin now has keys of its own for it, Shift+H and
Shift+U, bindable like the rest. What they rest on, from Zotero
10.0.3-beta.3's `resource/reader/reader.js`:

- The bare keys live in the view's keydown (82110-82116), under
  `readAloudActive && !event.target.matches('button, select')`: a focus on
  any button — the plugin's Player included — swallows them before they
  are looked at. The annotate button of Zotero's hidden popup made the
  same call (38623-38628).
- The call is two steps: `_readAloudManager.getSegmentToAnnotate()` —
  the previous segment while under half and under 3 s into the current
  one (39386-39395; the Engine answers it from `session.ts`) — then
  `reader.addAnnotationFromReadAloudSegment(segment, type)` (84337). That
  one finds the segment by `this._readAloudManager.segments.indexOf`, so
  it must be the manager's own object: the Engine's controller holds the
  reader's segments waived (`controller.ts`), which unwrap to the same
  objects on the way back.
- The annotation is created at once (`_addAnnotationFromReadAloudSegments`),
  not on the popup's confirm; the popup is `readAloudState.annotationPopup`.
  A second call while it is open only retypes it
  (`setReadAloudAnnotationType`, 84553). A segment that already carries an
  annotation (`segmentAnnotations`) reopens that one's popup instead of
  adding another.

## Document voices are separate settings records, and native auto-activation needs a guard (2026-09-26 12:52, issue #146)

Zotero 10.0.3's reader bundle calls `activate()` at 83876 from
`_onReadAloudEngineStateChanged` whenever the popup is open, a voice is
selected, and the manager is inactive. `activate()` (82550) requests
segments; `_createController()` (82653) can also run after segments arrive
or a jump. A guard only on the Player's Play button would leave native
fallback speech possible. The memory adapter guards activation and
controller creation, and clears the unavailable fallback's selected ID so
native auto-activation cannot spin. The saved document voice stays intact.

The native `setReadAloudVoices()` (84332) updates the reader's state only;
it does not itself change an active manager. The adapter's session snapshot
therefore keeps a remote merge or settings default change out of ongoing
reading, while the next activation restores the latest document record.
The sandbox diagnostic `documentVoices()` exposes the default, records and
reader selections. Product and storage decisions are in design/ADR 0009.

A document key uses the personal-library namespace or a group ID plus the
attachment key. Local library IDs differ across machines; the EPUB
content-derived Document Id would collapse separate attachments. Records
are separate small preferences, enumerated into backups and merged one
at a time in the existing shared settings file. Manual intent outranks
inheritance before modification time is compared.

## Independent voices verified through the player and isolated settings sync (2026-09-26 13:58, issue #146)

The tester verified 1.15.2-beta2, commit `30d5770`, through the bridge with
Fish voices Aarav and Default. Two unopened-player fixtures retained Aarav
after settings chose Default, while a third attachment inherited Default.
The player's dropdown and trusted voice shortcut wrote manual document
choices; the handoff preview left the inherited record untouched, and a
same-value pick advanced its timestamp. Close/reopen, an in-place reinstall
and a separate reader window retained the same attachment choice.

An isolated WebDAV sync adopted Aarav into the saved record while a
playing or paused session kept Default. Closing and reopening the player
selected Aarav. A manual record beat a newer inherited record, another
document adopted its newer manual record independently, and the upload
retained both. The settings UI exported all three records and the default;
restore reproduced them with sync suspended. A file without document
records left them untouched.

The missing-voice and no-default paths showed their prompts and created
no controller. Fish timing-log counts stayed unchanged; these were not
HTTP request counters. The first backgrounded player attempt hit an
autoplay block; the focused retry passed. Closing the separate reader
window produced Zotero-owned dead-object entries; the final run left no
plugin product error. The test server's shared settings file was restored
with HTTP 204, all three fixtures and their voice records were removed,
and the original settings and migrated owner default were restored. No
OpenReader Position add-on was installed. The retained scripts and the
issue report carry the full checks; voice quality remains human-only.

## Fish Default was an unconditional catalog entry, independent of every source switch (2026-09-26 14:26, issue #147)

Fish Audio's catalog appended `mul/default` after merging official, own
and manual voices. It was never returned by Fish Official: the entry
selects synthesis without a `reference_id`. The connection probe also
uses it, independently of the selectable catalog. Fish Speech lists
server references and has no equivalent appended entry.

The eight source-combination tests reproduced the unconditional entry.
The catalog now gates it on `includeOwn`, including when the account has
no custom voices; disabled-source cache and outage tests cover its
absence with Your voices off. Its ID and synthesis path stay the same.
The document-voice unavailable guard from #146 already preserves saved
choices and blocks automatic substitution; no migration is needed.

## Verified live: Default follows Your voices, including an empty catalog (2026-09-26 14:56, issue #147)

The tester installed `1.15.2-beta3` from `73617b6` and verified the
`includeOwn` gate in the installed bundle. All 25 startup steps passed.
The eight source combinations contained Default exactly when Your voices
was on: official-only returned 338 voices, own-only one, all sources 340,
and all off zero. Cached own-off/own-on switched 339 → 338 → 339.

With all sources off, both Test connection and the actual Enable action
reported zero voices and successful synthesis; enabling locked the source
controls without adding Default. A fixture with a saved Default choice
showed the unavailable prompt and no controller when Your voices was off.
Reopening after restoring the source selected the same Default again.
Fish timing-log counts stayed unchanged on the unavailable path; these
are result logs, not HTTP request counters.

The dedicated WebDAV destination matched. OpenReader Position was absent.
After fixture cleanup, position database/store/local counts were all 85,
store deletions 19, and shared documents/items 26/10, matching the baseline.
Transport and write queues were idle before automatic sync was restored.
The owner's existing reader was untouched. The retained Fish Audio kit
records the checks, preparation and restoration.

The full unit suite exercised 2,733 tests. Heavy host load exposed existing
timing-sensitive tests (the five-second build and word-onset checks and a
20 ms Fish request deadline); the three affected files passed all 94 tests
when rerun in one worker with a 30-second test timeout. Typecheck, build,
translation checks and documentation rendering passed.

The first live pass snapshotted Zotero's native `reader.readAloudVoices`
under the plugin namespace by mistake. No original native snapshot exists,
so that pass cannot prove the owner's native value was restored. The kit
now uses `extensions.zotero.reader.readAloudVoices`; a subsequent isolated
baseline/cleanup roundtrip matched the current value, type and user-value
flag exactly. This audit does not recover or prove the initial value.

## Remaining time uses the audio clock, and sections share SDT refs with segments (2026-09-26 15:38, issue #148)

Static inspection of the installed Zotero 10.0.3 reader bundle found that
`SDTView._convertOutline` maps `structure.catalog.outline` entries to
`#sdt-` plus their dot-joined refs (`resource/reader/reader.js`,
80133–80146). Segments retain the same nested ref path in
`position.start`, followed by the text-node and character offsets
(71176–71248; the shared-position investigation of 2026-09-21 recorded the
coordinate shape). The feature reads the source outline directly from
`_internalReader._sdt.structure.catalog.outline`, caches boundaries by
outline and segment identity, and accepts only ordered starts that map to
segments without crossing an earlier sentence. A PDF page destination or
an EPUB spine item alone cannot establish that boundary.

`EngineSession.segmentEnd` resets the position when a run completes, and
clears its forward stop when a selection completes. A separate completion
flag preserves “Finished” through that reset; on the next Play, a cleared
forward stop also changes the estimate's label from selection to document.
The session computes clip remainder from the audio clock and includes the
actual outstanding gap. Pausing during a gap drops it, as the Engine
already did; the estimate must not retain silence that will never play.

The estimate's prefix sums are built lazily, and measured clip lengths
survive the 32-clip cache's eviction. Updating the visible minutes does not
walk the document or request audio. The initial language heuristic is a
rough prior; samples from the active voice refine it. It is independent of
word highlighting and produces no word timings. The product trade-off is
recorded in design/ADR 0010.

The owner requested local checks only and explicitly deferred
`zotero-tester` and all zotero-dev calls. Runtime compartment access,
chapter extraction on real documents, and layout geometry remain for the
later authorized live run. No live verification is claimed here.

Local verification passed: 159 test files / 2,748 tests with
`npm test -- --maxWorkers=2`, typecheck, build and documentation rendering.
One default-concurrency run exceeded the existing five-second startup test
limit; the reduced-concurrency run passed without changing that limit.
The built XPI is `1.15.2-beta4`, not installed. Feature changes remain
uncommitted until the owner's next command.

## The remaining-time row must shorten the name without hiding the duration (2026-09-26 16:52, issue #148)

The first live kit reported a long-title row present and a 129-character
full tooltip, but measured the container rather than the duration's visible
bounds. The whole-line ellipsis shortened both the section name and its
trailing time. The Player now receives the localized full label plus
separate name and duration fields: only the name shrinks, while the time
keeps its width. Floating and docked layouts use the same row. The follow-up
must measure the duration span itself, not infer visibility from text
content or the tooltip.

Two first-pass voice rows were not testable because the kit wrote the old
`{speed, voice}` memory shape into `readAloud.defaultVoice`, whose shape is
`{id, lang}`, and tried to change an opened document through defaults and
private manager fields. The tester is correcting the driver to use the
Player and to wait for a paused voice handoff until Play. Fetch latency
(`store.msPerChar`) cannot demonstrate a changed speaking pace; that check
must compare actual clip duration and the remaining-time estimate.

Another worktree already used `1.15.2-beta5`, so the corrected time display
uses `1.15.2-beta6`. Installed artifact identity, not the version alone,
remains the verification boundary.
